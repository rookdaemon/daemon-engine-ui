# daemon-engine gateway URL (default: http://localhost:8080)
VITE_DAEMON_URL=http://localhost:8080

# Bearer token for authentication (must match the token configured in daemon-engine hooks)
# Example: if your daemon.yaml has hooks.webchat.token = "my-secret-token", set this to "my-secret-token"
VITE_DAEMON_TOKEN=your-token-here
